---
id: F-303
lane: content-claims
severity: major
---

# Title

Public code link missing from the case page even though the GitHub repository is public.

# Where

- Live: https://chatbots.ambern.dev/selected-work/small-business-chatbots (entire page — no link exists)
- Source: `app/selected-work/small-business-chatbots/page.tsx` (no GitHub reference anywhere in the component)

# Reproduction

1. `curl -s -o /dev/null -w "%{http_code}" https://github.com/amberndev/small-business-chatbots` → `200` for an anonymous request, so the repository is public (GitHub returns 404 to anonymous users for private repos).
2. Search the live case page HTML for "github" → no match.
3. `grep -rin github` across `app/`, `components/`, `content/`, `docs/` → the only hit is `docs/deploy/RUNBOOK.md:8` (internal deploy runbook), nothing user-facing.

# Expected

Contract section 12: "Link para código público somente se o repositório puder ser publicado." (Link to public code, only if the repository can be published.) The repository IS published/public, so the conditional is satisfied and the link is required.

# Actual

No link to https://github.com/amberndev/small-business-chatbots (or any code repository) appears anywhere on the case page, in the source component, or on the demo pages.

# Evidence

- `curl -s -o /dev/null -w "%{http_code}" https://github.com/amberndev/small-business-chatbots` → 200 (verified 2026-09-06).
- `grep -i github` over the downloaded live HTML → 0 matches.
