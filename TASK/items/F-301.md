---
id: F-301
lane: content-claims
severity: major
---

# Title

Case page has no real screenshots — the "Screenshots" requirement of contract section 12 is unfulfilled (prose placeholder only).

# Where

- Live: https://chatbots.ambern.dev/selected-work/small-business-chatbots — section "04 / VERIFICATION"
- Source: `app/selected-work/small-business-chatbots/page.tsx:15`

# Reproduction

1. `curl -sL https://chatbots.ambern.dev/selected-work/small-business-chatbots`
2. Grep the HTML for `<img` tags: only two occurrences, both `<img src="/ambern-logo.svg" alt="Ambern"/>` (header and footer logo).
3. Read the "04 / VERIFICATION" section text.

# Expected

Contract section 12 requires the case page to present: "Screenshots reais após a implementação." (Real screenshots after implementation.)

# Actual

No product screenshot is rendered anywhere on the page. The verification section only contains the sentence: "Screenshots are added after browser verification; no mockup is presented as a product capture." — i.e., an honest deferral, but the required screenshots are absent. Source (`page.tsx:15`): `<p className="muted">Screenshots are added after browser verification; no mockup is presented as a product capture.</p>`

# Evidence

- `grep -o "<img[^>]*>" case.html` → `<img src="/ambern-logo.svg" alt="Ambern"/>` ×2, nothing else.
- Note: another QA lane is producing the screenshots; this finding records the current live state. The honest framing (no fake mockups) complies with the no-invented-content rules — only the completeness item is open.
